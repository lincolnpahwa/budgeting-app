import chartsLineChart from 'budgeting-app/utils/charts/line-chart';
import { module, test } from 'qunit';

module('Unit | Utility | charts/line-chart', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = chartsLineChart();
    assert.ok(result);
  });
});
